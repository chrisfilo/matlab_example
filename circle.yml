## Customize dependencies
dependencies:
  pre:
    - yes y | sudo add-apt-repository ppa:octave/stable && sudo apt-get update && sudo apt-get -y install octave
    - octave --eval "pkg install -forge -auto statistics"
## Customize test commands
test:
  override:
    - octave age_demean.m